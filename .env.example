# ── Database ──────────────────────────────────────────────
# For local dev (host machine):
DATABASE_URL="postgresql://biztracker:biztracker_secret@localhost:5433/bizexpensetracker?schema=public"
# These are used by docker-compose for the Postgres container:
POSTGRES_USER="biztracker"
POSTGRES_PASSWORD="biztracker_secret"
POSTGRES_DB="bizexpensetracker"
DB_PORT="5433"

# ── NextAuth ──────────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="change-me-to-a-random-string-at-least-32-chars"
# Set to your server's URL (HTTPS via Caddy, e.g. https://192.168.1.31)
NEXTAUTH_URL="https://localhost"

# ── App ───────────────────────────────────────────────────
UPLOAD_DIR="./uploads"

# ── Plaid (optional) ─────────────────────────────────────
# https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=""
PLAID_SECRET=""
PLAID_ENV="sandbox"          # sandbox | development | production
